# DuckIP CLI

```powershell
npx -y duckip-cli --help
```

本地开发可运行 `node bin/duckip.js --help`。凭据使用 `DUCKIP_APP_KEY`、`DUCKIP_TOKEN`、`DUCKIP_PASSWORD`，或由 `auth key` / `auth login` 保存到用户配置目录。

变更订单、支付、账户、白名单和代理提取前，先使用 `--dry-run`；非交互环境需要 `--yes`。
